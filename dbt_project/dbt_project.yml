name: 'people_analytics'
version: '1.0.0'
config-version: 2

profile: 'people_analytics'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  people_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
